import buttonPreset from './buttonPreset';
import inputTextPreset from "./inputTextPreset";
import inputGroupPreset from "./inputGroupPreset";
import inputGroupAddonPreset from "./inputGroupAddonPreset";

const customPresets = {
    button: buttonPreset,
    inputText: inputTextPreset,
    inputGroup: inputGroupPreset,
    inputGroupAddon: inputGroupAddonPreset,
};

export default customPresets;